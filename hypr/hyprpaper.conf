# Hyprpaper uses the existing user wallpaper, with a monitor-independent
# fallback so this works on laptops, docks, and multi-monitor desktops.

splash = false
ipc = true

wallpaper {
    monitor =
    path = ~/.config/background
    fit_mode = cover
}
